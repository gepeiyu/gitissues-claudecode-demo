# Claude Code 治理规则

## 核心原则

- 所有工作必须从 GitHub Issue 开始
- 没有批准的实施计划，不得开始编码
- 不得直接在 main 分支上开发
- 不得自动合并 Pull Request
- 保持改动最小化、聚焦
- 范围变更和危险操作必须获得人工批准

---

# 1. Issue 规则

所有开发必须源自 GitHub Issue。

每个 Issue 应包含：

- 目标
- 需求
- 验收标准
- 非目标
- 技术说明（如有需要）

非目标示例：

- 不重构 auth 模块
- 不引入数据库
- 不修改 API 响应格式

如果需求不清晰：
- 先提问
- 不要猜测

---

# 2. 计划规则

实施前：

1. 完整阅读 Issue
2. 分析需求
3. 制定实施计划
4. 将计划发布到 Issue 评论中
5. 等待人工确认
6. 确认后才开始开发

不得立即开始编码。

实施计划必须包含：

- 需要创建的文件
- 需要修改的文件
- 测试计划
- 风险
- 范围外事项
- 预期修改的文件列表

如果实施过程中需要修改额外文件：
- 说明原因
- 先请求批准

---

# 2.1 状态更新规则

**开始实施前：**
1. 在 Issue 中添加"状态更新：开始实施"评论，说明将要进行的工作
2. **然后才开始编写代码**

**实施完成后：**
1. 在 Issue 中添加"状态更新：已完成，等待确认"评论，包含：
   - 修改/创建的文件列表
   - 测试结果
   - 验收标准核对清单
2. 等待用户确认实现是否完全满足需求
3. **只有用户主动要求创建 PR 时，才创建 Pull Request**

**禁止：**
- 未在 Issue 中更新开始状态就开始编码
- 未经用户确认自动创建 PR

---

# 3. 范围控制规则

未经批准不得扩大范围。

如果发现需要额外重构或重新设计：

1. 停下来
2. 说明原因
3. 描述影响
4. 请求批准
5. 确认后才继续

优先选择满足 Issue 需求的最简实现。

不得引入：

- 不必要的抽象
- 通用框架
- 过早的扩展性设计
- 架构重新设计

---

# 4. Git 规则

不得直接在 `main` 分支开发。

始终创建 feature 分支：

```bash
git checkout -b feature/issue-<id>-<short-name>
```

示例：

```bash
feature/issue-12-users-api
feature/issue-18-login-fix
```

Commit 应当：

- 小而聚焦
- 逻辑清晰

使用 Conventional Commits 格式：

```bash
feat(users): add name filtering
fix(auth): handle expired token
```

---

# 5. 危险命令规则

未经明确批准，不得执行危险命令。

以下命令未经确认禁止执行：

```bash
git push --force
git reset --hard
git clean -fd
rm -rf
git rebase
```

如果确实需要执行：
- 说明原因
- 说明风险
- 等待批准

---

# 6. 开发规则

推荐工作流程：

1. 根据验收标准编写测试
2. 运行测试 — 确认是失败状态
3. 向人工汇报测试用例，等待确认
4. 实现功能
5. 运行测试 — 确认全部通过
6. 自我审查改动
7. 创建 PR

开发过程中：

- 保持改动最小化
- 避免修改无关文件
- 避免隐蔽的行为变更
- 避免静默升级依赖

不得修改无关文件。

未经明确要求，不得删除文件。

**TDD 完整性规则：**

不得修改测试期望值来让测试通过。
不得删除或注释掉失败的测试用例。
不得使用 `skip` 或 `only` 绕过失败测试。
不得降低验收标准来迎合有问题的实现。

测试必须覆盖：

- 正常路径
- 边界输入（空值、最大值、特殊字符）
- 非法输入（缺少字段、类型错误）

如果在不违反以上规则的前提下无法让测试通过：
- 停下来
- 汇报冲突所在
- 等待人工决策

---

# 7. 失败处理规则

如果测试或实现反复失败：

连续失败 3 次后：

1. 停止实施
2. 汇报当前状态
3. 说明根本原因
4. 列出已尝试的修复方法
5. 等待人工决策

不得无限循环重试。

---

# 8. Pull Request 规则

所有改动必须经过 Pull Request 审查。

不得自动合并。

PR 创建后：
- 停下来
- 等待人工审查

优先使用小型 Pull Request。

如果实现过大：
- 拆分为多个阶段
- 或提议多个 PR

PR 描述应包含：

- 摘要
- 关联 Issue
- 已批准计划的链接
- 测试结果
- 风险
- 已知限制

---

# 9. Review 规则

Review 必须验证：

- 实现与已批准计划一致
- 验收标准已满足
- 没有额外扩大范围
- 没有修改无关文件
- 测试覆盖充分
- 风险可接受

如果实现偏离计划：
- 说明原因
- 请求批准

---

# 10. 决策记录规则

重要的技术决策应记录在 Issue 评论中。

使用以下结构：

```text
决策：
原因：
权衡：
```

示例：

- 选择内存存储
- 不使用 ORM
- 推迟缓存实现
- 简化架构

---

# 11. 完成标准规则

任务完成的条件：

- 测试全部通过
- 验收标准已验证
- 实现与已批准计划一致
- PR 已创建
- 已准备好 Review

实现代码本身不等于完成。

---

# 12. 人工权限规则

人工负责：

- 范围决策
- 架构决策
- 合并决策
- 生产环境决策
- 优先级决策

Claude 负责：

- 实现
- 测试
- 已批准范围内的局部重构
- 代码建议

权责不明时：
- 先提问

---

# 13. 沟通规则

简洁直接。

避免不必要的解释。

主动汇报：
- 风险
- 阻塞
- 假设
- 权衡

不确定时：
- 实施前先提问
