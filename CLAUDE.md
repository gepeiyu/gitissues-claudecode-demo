# CLAUDE.md

Issue 跟踪与 PR 工作流行为规范。
可按需与项目专属指令合并使用。

**取舍：** 正确性和人工确认优先于速度。不得跳过任何确认步骤。

---

## 1. 编码前先思考

**不做假设。不隐藏困惑。主动暴露权衡。**

- 明确说出假设。如有不确定，先问。
- 存在多种理解时，逐一列出——不要自己悄悄选一个。
- 有不清楚的地方，停下来。指出困惑所在，然后提问。

---

## 2. Issue 工作流

### 2a. 创建或编辑 Issue

**在用户确认草稿之前，不得向 GitHub 写入任何内容。**

结合代码库和现有 issue 分析请求，推断缺失字段：祈使句标题、标签、关联 issue、里程碑、负责人。

正文根据 issue 类型判断——不套固定模板。Bug 报告需要复现步骤和预期/实际行为对比；功能请求需要动机和验收标准；维护任务需要范围和完成定义。

将草稿明确标注后展示给用户，等待明确确认后再执行 `gh issue create` 或 `gh issue edit`，并汇报 URL。

---

### 2b. 实现 Issue

**先规划，发状态评论，再写代码。**

用 `gh issue view <编号>` 完整读取 issue。分析相关文件、测试覆盖和依赖关系。制定实现计划：步骤、每步的验证方式、涉及文件、风险项。展示计划，等待明确确认。

确认后，先拉取最新代码，从 `main` 新建分支，再动任何代码。命名格式为 `<类型>/issue-<编号>-<简短描述>`，前缀按工作性质选择：`feat/`、`fix/`、`chore/`、`docs/` 等。严禁直接向 `main` 提交。

在 issue 上发评论，记录已确认的计划、分支名称，状态标记为进行中并附当前时间。

在该分支上实现：条件允许时先写测试再改生产代码，每次有意义的改动后运行测试套件，完成前审查自己的 diff。只碰计划要求的内容。

完成后发第二条评论：每个文件的改动内容和原因、测试结果、分支名称，状态标记为已完成等待审查。不得自行关闭 issue。

---

## 3. Pull Request 工作流

**使用项目模板，关联 issue，确认后再创建。**

从分支名或近期提交中确认关联 issue 编号。检查 `.github/PULL_REQUEST_TEMPLATE.md` 和 `.github/PULL_REQUEST_TEMPLATE/` 目录。存在多个模板时询问用户选哪个；没有模板时自行组织正文：内容与原因、改动列表、测试方式、关闭引用。

每个部分填入真实内容，不得留占位符。展示草稿，等待明确确认后再执行 `gh pr create`。正文必须包含 `Closes #<编号>`。

---

## 4. 简洁与外科手术式修改

**用最少的代码解决问题，只碰必须碰的地方。**

- 不添加需求之外的功能。
- 不做投机性抽象或"为未来扩展"的设计。
- 不改动相邻代码、注释或格式。
- 沿用现有代码风格，即使你自己会写得不一样。
- 发现无关问题，说出来——不要悄悄修掉。
- 只删除你的改动导致不再使用的 import、变量或函数。

每一行改动都应当能直接追溯到当前实现的 issue。